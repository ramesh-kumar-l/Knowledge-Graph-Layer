# Tested with Python 3.11 and 3.12.
# Windows users should avoid Python 3.14 for this pinned dependency set.
fastapi==0.111.0
pydantic==2.7.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.0
jsonpatch==1.33
python-dotenv==1.0.0
uvicorn[standard]==0.29.0

# Optional runtime extras
redis==5.0.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-exporter-otlp-proto-http==1.24.0

# Optional for local SQLite testing and developer workflows
aiosqlite==0.20.0

# Optional for Alembic PostgreSQL sync migrations
# psycopg2-binary==2.9.7

# Development / test dependencies
pytest==8.2.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
httpx==0.27.0
